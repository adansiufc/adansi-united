import React from "react";
import WidthConstraint from "../ui/width-constraint";

const Gallery = () => {
  return (
    <section>
      <WidthConstraint>gallery</WidthConstraint>
    </section>
  );
};

export default Gallery;
